{% extends 'base-backend.html.twig' %}

{% block title %}Modifier la vidéo
{% endblock %}

{% block body %}
	<a href="{{ path('app_admin_video_index') }}" class="btn btn-dark mb-3">
		<i class="align-middle" data-feather="arrow-left-circle"></i>
	</a>
	<div class="d-flex flex-wrap justify-content-between align-items-center">
        <h1>Modifier la vidéo</h1>
        {{ include('admin_video/_delete_form.html.twig') }}
	</div>
	{{ include('admin_video/_form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}
