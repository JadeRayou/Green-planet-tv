<form method="post" action="{{ path('app_admin_video_delete', {'id': video.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ video.id) }}">
    <button class="btn btn-danger">Supprimer<i class="align-middle" data-feather="trash-2"></i></button>
</form>
